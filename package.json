{
  "name": "task-manager-ci",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "test": "node -e \"const {tasks} = require('./index.js'); if(tasks.length > 0) { console.log('Data Check Passed'); process.exit(0); } else { process.exit(1); }\"",
    "build": "echo 'Compressing assets and checking syntax...'"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
